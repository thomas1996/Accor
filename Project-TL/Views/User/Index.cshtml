@model IEnumerable<Project_TL.Models.Domain.User>

@{
    ViewBag.Title = "Index";
}
<html>
<body>


    <br />
    <br />
    <br />

    <h2>Users</h2>

    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Username)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Admin)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Username)
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.Location)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Admin)
                </td>
                <td>
                    @Html.ActionLink("Delete", "Delete","User", new { username = item.Username  }, new { onclick = "return confirm('Are you shure you wish to delete this user?');" })


                    @Html.ActionLink("Reset", "Reset", new { /* id=item.PrimaryKey */ }) |
                    @*@Html.ActionLink("Delete", "Delete","User", new { username = item.Username  }, new { onclick = "return confirm('Are you shure you wish to delete this user?');" })
                        @using (Ajax.BeginForm(
                "Delete", "User", new AjaxOptions()
                {
                    UpdateTargetId = "username",
                    HttpMethod = "Post"
                }
    ))
                        {

                            <div class="form-actions no-color">
                                <input type="submit" value="Delete" class="btn btn-default" /> |

                            </div>
                        }*@
                </td>
            </tr>
        }

    </table>
    <div>

        <h1>Add new user</h1>

        @Html.Partial("Create",new Project_TL.ViewModels.UserViewModel(new Project_TL.Models.Domain.User()))
    </div>
</body>
</html>